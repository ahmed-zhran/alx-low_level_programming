#include <stdio.h>

/**
*main - program that prints its name, followed by a new line.
*@argc: arg
*@argv: arg
*Return: Always 0.
*/

int main(int agrc, char *argv[])
{
	prinf("%s\n", argv[0]);
	return (0);
}
